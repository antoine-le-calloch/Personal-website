import Title from "../components/Title";

const Projects = () => {
    return (
        <div>
            <Title {...{
                text: "Projects",
                subText: "All my projects are listed above"
            }} />
        </div>
    );
};

export default Projects;
